name: Genesis Deploy
on:
  push:
    branches: [ main ]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Deploy to Production
        run: |
          echo "Deploying EarnNexus to af-south-1 (Cape Town)"
          # Add ssh commands to trigger launch.ps1 on target Windows/Linux server
